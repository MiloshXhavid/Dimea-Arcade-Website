---
phase: 09-hardware-waitlist-newsletter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [09-01, 09-02, 09-03, 09-04, 09-05, 09-06]

must_haves:
  truths:
    - "Mailchimp account exists with one audience, double opt-in ON"
    - "Two Groups created in the audience: Hardware Waitlist and Newsletter"
    - "Embedded form action URL obtained from Mailchimp dashboard"
    - "Group field names (group[CATEGORY_ID][GROUP_ID]) for both groups obtained"
    - "Final Welcome Email enabled in Mailchimp audience settings"
    - "Dimitri can confirm all five items above before plan 02 executes"
  artifacts:
    - path: ".planning/phases/09-hardware-waitlist-newsletter/09-01-SUMMARY.md"
      provides: "Recorded Mailchimp action URL and group field names for plan 02"
  key_links:
    - from: "Mailchimp dashboard (Audience > Signup forms > Embedded forms)"
      to: "09-02-PLAN.md JS constants"
      via: "action URL and group IDs captured in SUMMARY"
      pattern: "MC_ACTION_URL|HW_GROUP_PARAM|NL_GROUP_PARAM"
---

<objective>
Verify Dimitri has completed all Mailchimp prerequisite setup before any code is written.

Purpose: Plan 02 cannot proceed without the real Mailchimp action URL and group field names. These are audience-specific values that only exist after Dimitri creates his Mailchimp account and groups. This checkpoint gates execution and captures those values for plan 02.

Output: Confirmed Mailchimp setup + captured values recorded in SUMMARY for plan 02 executor.
</objective>

<execution_context>
@C:/Users/Milosh Xhavid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Milosh Xhavid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hardware-waitlist-newsletter/09-CONTEXT.md
@.planning/phases/09-hardware-waitlist-newsletter/09-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Mailchimp setup prerequisite — Dimitri configures account</name>
  <what-built>Nothing built yet. This checkpoint captures the external values the code in plan 02 requires.</what-built>
  <how-to-verify>
    Dimitri must complete ALL of the following in his Mailchimp account before plan 02 can execute:

    1. Create a Mailchimp account at mailchimp.com (free plan)
    2. Create one Audience (any name — e.g. "DIMEA List")
    3. In Audience settings: enable double opt-in (Audience > Settings > Audience name and defaults > Enable double opt-in)
    4. In Audience > Manage contacts > Groups: create two groups in one category
       - Group 1: "Hardware Waitlist"
       - Group 2: "Newsletter"
       Note the GROUP CATEGORY NAME you choose — you'll need the group IDs
    5. Set From name: Dimitri | From email: contact@dimea.audio (Audience > Settings > Campaign defaults)
    6. Enable the Final Welcome Email: Audience > Signup forms > Form builder > Forms and response emails > Final Welcome Email > Enable
       Draft the welcome email content (see copy below in Task 2)
    7. Go to Audience > Signup forms > Embedded forms > Generate the form HTML
       Copy the entire generated HTML block
       From it, extract:
         a. The action URL (e.g. https://dimea.us1.list-manage.com/subscribe/post?u=XXX&amp;id=YYY)
         b. The group checkbox inputs — they look like:
            &lt;input type="checkbox" value="8" name="group[01245][8]" ...&gt;
            The numbers in group[CATEGORY_ID][GROUP_ID] are what you need
    8. Share with Claude:
       - Action URL (the post? URL from the form action attribute)
       - group[X][Y] field name for Hardware Waitlist group
       - group[X][Y] field name for Newsletter group
  </how-to-verify>
  <resume-signal>Paste the action URL and both group field names, e.g.:
    Action URL: https://dimea.us1.list-manage.com/subscribe/post?u=abc123&id=def456
    Hardware Waitlist group field: group[01234][1]
    Newsletter group field: group[01234][2]
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Draft Mailchimp Final Welcome Email copy</name>
  <files>.planning/phases/09-hardware-waitlist-newsletter/09-01-SUMMARY.md</files>
  <action>
    Write the Final Welcome Email copy that Dimitri will paste into Mailchimp's Final Welcome Email editor.
    This runs in parallel with the checkpoint — draft the copy, then include it in the SUMMARY.

    Welcome email spec:
    - Subject line: "You're in — here's what comes next"
    - From: Dimitri / contact@dimea.audio
    - Tone: warm, personal, founder-to-fan. Not corporate. Not brand-manifesto.
    - Length: 3–4 short paragraphs. No fluff.

    Email body to draft:

    ---
    Subject: You're in — here's what comes next

    Hey,

    Thanks for signing up — I'm Dimitri, the one person behind DIMEA.

    Here's what you signed up for: I'm building two things. Arcade Chord Control is a plugin that lets you play chords with a single key — no theory required, just your ear. DIMEOLA is the hardware version: a standalone device with a joystick and a chord engine, no DAW needed.

    You'll hear from me when the hardware campaign goes live and when new plugin versions ship. I send maybe two or three emails a year — only when there's something real to say.

    Thanks for being early.
    — Dimitri
    ---

    Tone notes applied:
    - No "We" — Dimitri writes as himself
    - No marketing adjectives ("revolutionary", "game-changing")
    - Explains both products simply in case subscriber found site via plugin vs hardware
    - Sets accurate expectation: low-frequency, high-signal emails

    After drafting the email copy, create 09-01-SUMMARY.md with:
    - Recorded Mailchimp action URL and group field names (once provided by Dimitri in the checkpoint)
    - The welcome email copy above (for Dimitri to paste into Mailchimp)
    - Instructions for where Dimitri pastes the email copy in Mailchimp
    - Note that plan 02 requires these values before execution

    SUMMARY format per .planning templates. Record the action URL and group params as JS constants ready for plan 02:

    ```javascript
    var MC_ACTION_URL = '[ACTION_URL_FROM_DIMITRI]'; // TODO: replace with real URL
    var HW_GROUP_PARAM = '&group[X][Y]=1'; // TODO: replace X and Y from Mailchimp form HTML
    var NL_GROUP_PARAM = '&group[X][Z]=1'; // TODO: replace X and Z from Mailchimp form HTML
    ```
  </action>
  <verify>
    .planning/phases/09-hardware-waitlist-newsletter/09-01-SUMMARY.md exists and contains:
    - Welcome email copy
    - JS constant stubs (MC_ACTION_URL, HW_GROUP_PARAM, NL_GROUP_PARAM) with placeholder values or real values if Dimitri provided them
  </verify>
  <done>SUMMARY exists with welcome email copy and JS constant stubs ready for plan 02 executor</done>
</task>

</tasks>

<verification>
Before marking plan 01 complete:
- [ ] Dimitri has confirmed Mailchimp account, audience, two groups, double opt-in ON, Final Welcome Email enabled
- [ ] Action URL obtained and recorded in SUMMARY
- [ ] Both group field names obtained and recorded in SUMMARY
- [ ] Welcome email copy drafted and included in SUMMARY with paste instructions for Dimitri
</verification>

<success_criteria>
Plan 01 complete when:
1. Checkpoint: Dimitri has confirmed setup and provided action URL + both group[X][Y] field names
2. SUMMARY.md exists with welcome email copy and JS constants populated with real values
</success_criteria>

<output>
After completion, create `.planning/phases/09-hardware-waitlist-newsletter/09-01-SUMMARY.md`
</output>
