---
phase: 08-lemonsqueezy-plugin-checkout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: false
requirements:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-05
  - 08-06

must_haves:
  truths:
    - "Clicking 'Buy Now' in the plugin section opens a LemonSqueezy overlay modal (no page navigation)"
    - "Clicking 'Buy Now' in the shop/pricing section opens the same LS overlay modal"
    - "Clicking 'Buy Now ->' in the sticky CTA opens the LS overlay modal"
    - "No email-collection form or checkout-toggle UI appears anywhere on the page"
    - "The page loads with zero JS console errors related to LemonSqueezy"
    - "A single LS_CHECKOUT_URL constant is defined — the only place to update the real URL"
  artifacts:
    - path: "index.html"
      provides: "LemonSqueezy overlay wiring for all 3 Buy Now touchpoints"
      contains: "lemonsqueezy-button"
    - path: "index.html"
      provides: "Lemon.js CDN script tag"
      contains: "app.lemonsqueezy.com/js/lemon.js"
    - path: "index.html"
      provides: "LS_CHECKOUT_URL constant"
      contains: "LS_CHECKOUT_URL"
  key_links:
    - from: "plugin section Buy Now anchor"
      to: "LS overlay"
      via: "lemonsqueezy-button class + href=LS_CHECKOUT_URL"
      pattern: "lemonsqueezy-button"
    - from: "shop section Buy Now (converted anchor)"
      to: "LS overlay"
      via: "lemonsqueezy-button class + href=LS_CHECKOUT_URL"
      pattern: "shop-btn.*lemonsqueezy-button"
    - from: "sticky CTA button"
      to: "LS overlay"
      via: "LemonSqueezy.Url.Open(LS_CHECKOUT_URL)"
      pattern: "LemonSqueezy\\.Url\\.Open"
    - from: "Lemon.js script"
      to: "LS overlay infrastructure"
      via: "defer script before </body>"
      pattern: "lemon\\.js.*defer"

user_setup:
  - service: lemonsqueezy
    why: "Plugin checkout — overlay requires a real checkout URL from the LS dashboard"
    env_vars: []
    dashboard_config:
      - task: "Create Arcade Chord Control product listing (Windows installer, CHF 49 launch / CHF 79 regular)"
        location: "LemonSqueezy Dashboard -> Products -> New Product"
      - task: "Copy the Checkout Overlay URL"
        location: "LS Dashboard -> Product -> Share -> Checkout Overlay — format: https://[STORE_SLUG].lemonsqueezy.com/checkout/buy/[VARIANT_UUID]"
      - task: "Paste the URL into index.html as the value of LS_CHECKOUT_URL (marked with TODO comment)"
        location: "index.html — const LS_CHECKOUT_URL = '...' near the sticky CTA script block"
---

<objective>
Wire LemonSqueezy overlay checkout into index.html so visitors can buy Arcade Chord Control without leaving the page.

Purpose: Replace the existing placeholder email-collection form with real LS-powered checkout. Three buy-button touchpoints (plugin section, shop section, sticky CTA) all open the same LS overlay modal. LS handles payment, license key generation, and download link delivery automatically.

Output: A modified index.html where all "Buy Now" buttons trigger the LS overlay. Old custom checkout form and its JS toggle block are removed. Lemon.js CDN script loaded before </body>.
</objective>

<execution_context>
@C:/Users/Milosh Xhavid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Milosh Xhavid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key HTML structure the executor needs. Extracted from index.html. -->

Line 1055 — Plugin section buy buttons (inside .price-actions):
```html
<div class="price-actions">
  <a href="#" class="btn-primary">Buy Now — CHF 49</a>
  <a href="#" class="btn-secondary">Try Free</a>
</div>
```

Lines 1568–1582 — Shop section button + email form (FULL BLOCK TO REMOVE/REPLACE):
```html
<button class="shop-btn shop-btn--primary" id="buyPluginBtn">Buy Now — CHF 49 -></button>
<div class="shop-checkout" id="pluginCheckout">
  <div class="checkout-label">Enter your email — we'll send the download link instantly.</div>
  <div class="checkout-row">
    <input class="checkout-input" type="email" placeholder="your@email.com" id="checkoutEmail" />
    <button class="checkout-pay" id="checkoutPay">Pay CHF 49</button>
  </div>
  <div class="checkout-methods">...</div>
  <div class="checkout-note">Secure checkout · Instant delivery · Lifetime licence · Free updates</div>
</div>
```

Lines 2404–2425 — Checkout toggle JS block (FULL BLOCK TO REMOVE):
```javascript
// ── PLUGIN CHECKOUT TOGGLE ──────────────────────────────────────
const buyBtn = document.getElementById('buyPluginBtn');
const checkout = document.getElementById('pluginCheckout');
if (buyBtn && checkout) { ... }
```

Line 4054 — Sticky CTA button (onclick attribute to REMOVE):
```html
<button class="sticky-cta-btn" onclick="document.getElementById('buyPluginBtn').click(); ...">Buy Now -></button>
```

Lines 4073–4075 — End of file (Lemon.js script goes BEFORE </body>):
```html
</script>
</body>
</html>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old checkout form and wire LemonSqueezy overlay</name>
  <files>index.html</files>
  <action>
Make all five changes to index.html in order:

**Change 1 — Plugin section "Buy Now" button (line ~1055):**
Add `lemonsqueezy-button` class and set `href` to the LS_CHECKOUT_URL placeholder on the existing `.btn-primary` anchor. The href value must match the `LS_CHECKOUT_URL` constant defined in Change 4. Result:
```html
<a href="https://YOUR-STORE.lemonsqueezy.com/checkout/buy/YOUR-VARIANT-UUID"
   class="btn-primary lemonsqueezy-button">Buy Now — CHF 49</a>
```
The "Try Free" anchor stays unchanged but add a comment above it: `<!-- TODO: replace href="#" with Windows demo download URL when available -->`. Do NOT remove the Try Free anchor.

**Change 2 — Shop section (lines ~1568–1582):**
Replace the `<button id="buyPluginBtn">` element AND the entire `<div class="shop-checkout" id="pluginCheckout">` block (lines 1569–1582) with a single anchor:
```html
<a href="https://YOUR-STORE.lemonsqueezy.com/checkout/buy/YOUR-VARIANT-UUID"
   class="shop-btn shop-btn--primary lemonsqueezy-button">Buy Now — CHF 49 -></a>
```
The `<div class="trust-badge">` on line 1583 stays unchanged.

**Change 3 — Remove checkout toggle JS (lines ~2404–2425):**
Delete the entire `// ── PLUGIN CHECKOUT TOGGLE` block including the comment header and the closing `}` at line 2425. The blank line before the `// Scroll reveal` comment at line 2427 can stay for readability. Do NOT touch the scroll reveal block or any other JS.

**Change 4 — Sticky CTA button onclick + LS_CHECKOUT_URL constant:**
In the `<script>` block that begins at line 4058 (the `// ── STICKY PLUGIN CTA ──` IIFE block), add the following directly AFTER the closing `})();` of the IIFE and BEFORE the closing `</script>` tag:

```javascript
// TODO: Replace with real URL from LS Dashboard -> Product -> Share -> Checkout Overlay
const LS_CHECKOUT_URL = 'https://YOUR-STORE.lemonsqueezy.com/checkout/buy/YOUR-VARIANT-UUID';

// ── LEMONSQUEEZY OVERLAY WIRING ──────────────────────────────────
const stickyCTAbtn = document.querySelector('.sticky-cta-btn');
if (stickyCTAbtn) {
  stickyCTAbtn.addEventListener('click', () => {
    LemonSqueezy.Url.Open(LS_CHECKOUT_URL);
  });
}

LemonSqueezy.Setup({
  eventHandler: (event) => {
    if (event.event === 'Checkout.Success') {
      document.querySelectorAll('.lemonsqueezy-button, .sticky-cta-btn').forEach(el => {
        el.textContent = 'Purchase Complete';
      });
    }
  }
});
```

Also remove the `onclick` attribute from the sticky CTA button entirely — the addEventListener above replaces it and removing onclick prevents double-firing:
```html
<button class="sticky-cta-btn">Buy Now -></button>
```

**Change 5 — Lemon.js script tag:**
Add the Lemon.js script tag immediately before `</body>`. Place it after the sticky CTA `</script>` block (line 4073) and before `</body>` (line 4074):
```html
<script src="https://app.lemonsqueezy.com/js/lemon.js" defer></script>
```

**Critical anti-patterns to avoid:**
- Do NOT add `lemonsqueezy-button` to any `<button>` element — it only works on `<a>` tags
- Do NOT use `async` instead of `defer` on the lemon.js script tag
- Do NOT use a `/checkout/?cart=` URL — only `/checkout/buy/` URLs are reusable across customers
- Do NOT leave any reference to `buyPluginBtn`, `pluginCheckout`, or `checkoutPay` IDs in HTML or JS
- All three placeholder URL strings must be identical: `https://YOUR-STORE.lemonsqueezy.com/checkout/buy/YOUR-VARIANT-UUID`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const lsBtn=(h.match(/lemonsqueezy-button/g)||[]).length;const lsJs=(h.match(/lemon\.js/g)||[]).length;const lsUrl=(h.match(/LS_CHECKOUT_URL/g)||[]).length;const noOld=!h.includes('pluginCheckout')&&!h.includes('checkoutPay');console.log('lemonsqueezy-button count:',lsBtn,'(expect 2)');console.log('lemon.js count:',lsJs,'(expect 1)');console.log('LS_CHECKOUT_URL count:',lsUrl,'(expect 4)');console.log('old form removed:',noOld);if(lsBtn===2&&lsJs===1&&lsUrl===4&&noOld)console.log('PASS');else console.log('FAIL');"</automated>
  </verify>
  <done>
    - index.html contains exactly 2 elements with class `lemonsqueezy-button` (both are anchor tags)
    - index.html contains the lemon.js CDN script tag with `defer` before `</body>`
    - index.html contains exactly 4 references to `LS_CHECKOUT_URL` (1 const def + 2 href values + 1 Url.Open call)
    - index.html contains zero references to `pluginCheckout`, `checkoutPay`, or `checkoutEmail`
    - sticky CTA button has no `onclick` attribute
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify LemonSqueezy overlay wiring in browser</name>
  <files>index.html</files>
  <action>Human verifies structural changes and overlay behavior in browser. See how-to-verify for steps.</action>
  <verify>
    <automated>MISSING — verification is manual browser smoke testing (no automated test framework in this project). See how-to-verify steps below.</automated>
  </verify>
  <done>Structural checks 1–7 pass: no console errors, email form gone, lemonsqueezy-button on anchor tags only, pluginCheckout not in DOM.</done>
  <what-built>
    LemonSqueezy overlay wired into all three Buy Now touchpoints. Old custom email-collection checkout form and its JS toggle block removed. Lemon.js CDN script added before </body>. Single LS_CHECKOUT_URL placeholder constant defined with TODO comment.
  </what-built>
  <how-to-verify>
    Structural checks (can be done now, before real LS URL is available):

    1. Open index.html in a browser (file:// or Vercel preview URL)
    2. Open DevTools Console — confirm zero red errors on page load
    3. Scroll to plugin section — confirm "Buy Now — CHF 49" and "Try Free" buttons are both present
    4. Scroll to shop/pricing section — confirm the email input form is GONE and replaced by a single "Buy Now — CHF 49 ->" link
    5. Scroll down until sticky CTA appears — confirm it shows "Buy Now ->" and clicking it does NOT scroll to the plugin section (old behavior: click scrolls page AND opens email form)
    6. In DevTools Elements, search for "pluginCheckout" — confirm zero results
    7. In DevTools Elements, search for "lemonsqueezy-button" — confirm exactly 2 results, both on anchor tags

    Real overlay checks (once LS product listing exists and real URL is pasted in):
    8. Replace all 3 placeholder URLs with the real URL from LS Dashboard -> Product -> Share -> Checkout Overlay
    9. Click "Buy Now" in plugin section — LS overlay modal opens (no page navigation)
    10. Click "Buy Now ->" in shop section — same LS overlay opens
    11. Click sticky CTA "Buy Now ->" — same LS overlay opens
    12. Confirm site stays on screen behind the overlay during checkout
  </how-to-verify>
  <resume-signal>Type "approved" if structural checks (steps 1–7) pass. Describe any issues found. Real URL overlay testing (steps 8–12) can happen separately once LS prerequisites are complete.</resume-signal>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -c "lemonsqueezy-button" index.html` returns 2
2. `grep "lemon.js" index.html` returns the defer script tag before </body>
3. `grep -c "LS_CHECKOUT_URL" index.html` returns 4 (1 const + 2 hrefs + 1 Url.Open)
4. `grep "pluginCheckout\|checkoutPay\|checkoutEmail" index.html` returns no matches
5. `grep "buyPluginBtn" index.html` returns no matches
6. Human browser verification: email form gone, no page errors, lemonsqueezy-button on anchors only
</verification>

<success_criteria>
- All three "Buy Now" touchpoints wired to LemonSqueezy overlay (2 via lemonsqueezy-button on anchor tags, 1 via LemonSqueezy.Url.Open)
- Old email-collection form (HTML + JS) fully removed — zero references to pluginCheckout/checkoutPay/checkoutEmail
- Lemon.js CDN script present before </body> with defer attribute
- Single LS_CHECKOUT_URL constant with TODO comment — one place to fill in the real URL once LS is set up
- Page loads with no JS errors
- Human browser verification approved (structural checks 1–7)
</success_criteria>

<output>
After completion, create `.planning/phases/08-lemonsqueezy-plugin-checkout/08-01-SUMMARY.md` using the summary template at `@C:/Users/Milosh Xhavid/.claude/get-shit-done/templates/summary.md`
</output>
