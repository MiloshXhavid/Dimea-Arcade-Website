---
phase: 10-site-reframe-plugin-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: false
requirements: []

must_haves:
  truths:
    - "Hero shows plugin as the primary CTA — no hardware preorder button visible"
    - "No CHF 499 price anywhere on the page"
    - "Hardware section reads as in-development vision, no purchase CTA"
    - "Shop shows only the plugin card, with a 'More coming' note below it"
    - "Desktop nav, mobile overlay, and footer nav all show 'Vision' and 'Roadmap' instead of 'Hardware' and 'App'"
    - "hw-waitlist form still present and intact"
    - "navHardware scroll-spy ID preserved — scroll-spy still works"
  artifacts:
    - path: "index.html"
      provides: "Entire reframed site — all changes are in-place edits to this file"
      contains: "btn-primary.*Get Plugin"
  key_links:
    - from: "hero-actions div (line 930)"
      to: "#pageShop"
      via: "btn-primary anchor href"
      pattern: "btn-primary.*pageShop"
    - from: "navHardware anchor"
      to: "#pageHardware"
      via: "id=navHardware preserved, only text changed"
      pattern: "id=\"navHardware\""
---

<objective>
Reframe index.html from a dual plugin+hardware sales page to a plugin-first site. The Arcade Chord Control is the one product for sale today. Hardware (DIMEOLA) and App become vision sections — no pricing, no deposit CTAs, no preorder language.

Purpose: Remove the contradiction of selling an unbuilt product alongside an available one. Make the hero and shop focused on the single purchasable item.
Output: Updated index.html with hero, hardware section, shop grid, and all nav labels reflecting the new framing.
</objective>

<execution_context>
@C:/Users/Milosh Xhavid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Milosh Xhavid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-site-reframe-plugin-first/10-CONTEXT.md
@.planning/phases/10-site-reframe-plugin-first/10-RESEARCH.md

<interfaces>
<!-- Key structural facts. All changes are content edits to index.html only. -->
<!-- Exact line numbers confirmed by direct file read on 2026-03-15. -->
<!-- Line numbers may drift slightly if earlier phases modified index.html. Grep to confirm before editing. -->

Current hero-actions block (~line 930):
```html
<div class="hero-actions">
  <a href="#" class="btn-primary">Preorder Hardware — CHF 499 <svg...></svg></a>
  <a href="#plugins" class="btn-secondary">Get Plugin — CHF 49</a>
  <button class="btn-ghost-text" id="exploreFreeHero" style="...">Explore Free →</button>
</div>
```

Current hw-status-bar (~line 1187):
```html
<div class="hw-status-bar">
  <div class="hw-status-dot"></div>
  <span class="hw-status-text">CONCEPT &amp; PROTOTYPE STAGE</span>
  <div class="hw-status-line"></div>
  <span class="hw-status-tag">Daisy Seed · Pure Data · ~CHF 499</span>
</div>
```

Hardware section-label (~line 1135): `<div class="section-label">Hardware</div>`

Hardware spec list Target price row (~line 1334):
`<li><span class="hw-spec-key">Target price</span><span class="hw-spec-val">~CHF 499</span></li>`

Hardware roadmap timeline last step description (~line 1380):
`"10–20 artist beta units. Kickstarter campaign targeting ~CHF 499. Early backers get direct input on the final spec. Est. delivery Q1 2028."`

Desktop nav (~lines 905, 908):
```html
<li><a href="#pageHardware" id="navHardware">Hardware</a></li>
<li><a href="#pageApp">App</a></li>
```

Section dividers (~lines 1126, 1640):
```html
<div class="section-divider"><span class="section-divider-num">03</span><span class="section-divider-name">Hardware</span>...
<div class="section-divider"><span class="section-divider-num">06</span><span class="section-divider-name">App</span>...
```

Mobile overlay (~lines 4006, 4009):
```html
<li><a href="#pageHardware">Hardware</a></li>
<li><a href="#pageApp">App</a></li>
```

Footer nav (~lines 1744, 1747):
```html
<li><a href="#pageHardware">Hardware</a></li>
<li><a href="#pageApp">App</a></li>
```

Shop grid (~lines 1542–1635): Three cards inside `.shop-grid`:
- `.shop-card--available` — plugin card (KEEP)
- `.shop-card--preorder` — hardware card (lines 1588–1610, REMOVE)
- `.shop-card--dim` — bundle card (lines 1612–1633, REMOVE)

Shop description (~line 1520):
`<p class="shop-desc">Plugin licenses, hardware pre-orders, and future drops — in one place.</p>`

App section badge (~line 1649): Already shows "IN DEVELOPMENT — iOS · Android · macOS" in amber. No purchase CTA present. Tone is already vision-framing — no content change needed beyond nav/section-label rename.

Scroll-spy JS array (lines ~1836–1840):
```javascript
[
  {id:'pageHome',        nav:'navPlugins'},
  {id:'pageAbout',       nav:'navAbout'},
  {id:'pageHardware',    nav:'navHardware'},
  {id:'pageImpressions', nav:'navImpressions'},
]
```
CRITICAL: id="navHardware" is used by JS. Change only the visible text, never the id attribute.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero CTA swap + hardware section price and CTA removal</name>
  <files>index.html</files>
  <action>
Make the following targeted edits to index.html. Read the file first to confirm current line numbers before editing (line numbers from research may have drifted if other phases ran).

**Hero hero-actions block (~line 930):**

REMOVE the entire hardware preorder `<a>` tag:
```html
<a href="#" class="btn-primary">Preorder Hardware — CHF 499 <svg width="14" height="10" viewBox="0 0 14 10" fill="none"><path d="M9 1L13 5L9 9M1 5H13" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg></a>
```

CHANGE the plugin `<a>` tag from btn-secondary to btn-primary, update href from `#plugins` to `#pageShop`, and append the arrow SVG from the removed button:
```html
<a href="#pageShop" class="btn-primary">Get Plugin — CHF 49 <svg width="14" height="10" viewBox="0 0 14 10" fill="none"><path d="M9 1L13 5L9 9M1 5H13" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg></a>
```

LEAVE the `btn-ghost-text` Explore Free button exactly as-is.

Result: hero-actions has exactly two children — btn-primary plugin link, btn-ghost-text Explore Free button.

**Hardware status bar (~line 1191):**

CHANGE the `.hw-status-text` and `.hw-status-tag` content:
- `CONCEPT &amp; PROTOTYPE STAGE` → `IN DEVELOPMENT`
- `Daisy Seed · Pure Data · ~CHF 499` → `Daisy Seed · Pure Data · Concept &amp; Prototype`

**Hardware section-label (~line 1135):**

CHANGE:
`<div class="section-label">Hardware</div>`
→ `<div class="section-label">Vision</div>`

**Hardware spec list Target price row (~line 1334):**

REMOVE the entire `<li>` for Target price:
`<li><span class="hw-spec-key">Target price</span><span class="hw-spec-val">~CHF 499</span></li>`

**Hardware roadmap timeline last step description (~line 1380):**

CHANGE the `.hw-step-desc` in the "Beta Units + Crowdfunding" step:
Current: `"10–20 artist beta units. Kickstarter campaign targeting ~CHF 499. Early backers get direct input on the final spec. Est. delivery Q1 2028."`
→ `"10–20 artist beta units. Kickstarter campaign — early backers get direct input on the final spec. Est. delivery Q1 2028."`

**Verify after this task (before committing):**
Run: `grep -n "499\|Preorder\|preorder" index.html`
Expected: 0 matches. If any remain, fix them before committing.

Do NOT touch: the hw-waitlist form, any `id` attributes, any CSS, any JavaScript.
  </action>
  <verify>
    <automated>grep -n "499\|Preorder\|preorder" index.html</automated>
  </verify>
  <done>
    - grep for "499" returns 0 matches in index.html
    - grep for "Preorder" / "preorder" returns 0 matches
    - hero-actions contains exactly: btn-primary plugin link + btn-ghost-text Explore Free
    - Hardware status bar reads "IN DEVELOPMENT" with no price in tag
    - Target price spec row is gone
    - Roadmap timeline last step has no CHF 499
  </done>
</task>

<task type="auto">
  <name>Task 2: Shop collapse + nav and section divider renames</name>
  <files>index.html</files>
  <action>
Make the following targeted edits to index.html.

**Shop section — collapse to single card:**

CHANGE the `.shop-desc` copy (~line 1520):
`Plugin licenses, hardware pre-orders, and future drops — in one place.`
→ `The plugin is available now. Hardware and app are in development — more products coming in 2026.`

REMOVE the entire hardware preorder card from `.shop-grid` (~lines 1588–1610):
```html
<!-- Hardware — pre-order -->
<div class="shop-card shop-card--preorder">
  ...all content...
</div>
```

REMOVE the entire bundle card from `.shop-grid` (~lines 1612–1633):
```html
<!-- Bundle — coming -->
<div class="shop-card shop-card--dim">
  ...all content...
</div>
```

ADD a `.shop-more-coming` paragraph after the closing `</div>` of the plugin shop card, still inside `.shop-grid`:
```html
<p class="shop-more-coming">More products in development — hardware and app coming in 2026.</p>
```

ADD a CSS rule for `.shop-more-coming` inside the existing `<style>` block in `<head>`. Place it near the existing `.shop-grid` rules. Use this rule:
```css
.shop-more-coming {
  font-family: 'DM Mono', monospace;
  font-size: 11px;
  letter-spacing: .15em;
  text-transform: uppercase;
  color: var(--text-faint);
  text-align: center;
  padding: 20px 0 0;
  grid-column: 1 / -1;
}
```

**Nav label renames — three locations:**

Desktop nav `<ul class="nav-links">` (~lines 905, 908):
- `id="navHardware"` link: change text from `Hardware` to `Vision`. PRESERVE `id="navHardware"` and `href="#pageHardware"` exactly.
- App link: change text from `App` to `Roadmap`. PRESERVE `href="#pageApp"` exactly.

Mobile overlay `<div id="navOverlay">` (~lines 4006, 4009):
- Hardware link: change text from `Hardware` to `Vision`. PRESERVE `href="#pageHardware"`.
- App link: change text from `App` to `Roadmap`. PRESERVE `href="#pageApp"`.

Footer nav `<ul class="footer-links">` (~lines 1744, 1747):
- Hardware link: change text from `Hardware` to `Vision`. PRESERVE `href="#pageHardware"`.
- App link: change text from `App` to `Roadmap`. PRESERVE `href="#pageApp"`.

**Section dividers (~lines 1126, 1640):**
- `<span class="section-divider-name">Hardware</span>` → `<span class="section-divider-name">Vision</span>`
- `<span class="section-divider-name">App</span>` → `<span class="section-divider-name">Roadmap</span>`

**App section-label (~line 1647):**
`<div class="section-label">App</div>` → `<div class="section-label">Roadmap</div>`

**Verify after this task (before committing):**
1. `grep -n "shop-card--preorder\|shop-card--dim" index.html` → 0 matches
2. `grep -c "id=\"navHardware\"" index.html` → exactly 1 (id preserved, not duplicated or removed)
3. `grep -n ">Hardware<\|>App<" index.html` → should return 0 visible label matches (href values containing "Hardware" / "App" as anchor targets are fine — check only the text node content)

Do NOT change any `id` attributes, any `href` values, or any JavaScript.
  </action>
  <verify>
    <automated>grep -n "shop-card--preorder\|shop-card--dim" index.html && grep -c "id=\"navHardware\"" index.html</automated>
  </verify>
  <done>
    - shop-card--preorder and shop-card--dim are gone from the DOM
    - .shop-grid contains exactly: plugin card + .shop-more-coming paragraph
    - .shop-more-coming CSS rule exists in the style block
    - Desktop nav shows "Vision" and "Roadmap" labels
    - Mobile overlay shows "Vision" and "Roadmap" labels
    - Footer nav shows "Vision" and "Roadmap" labels
    - Section dividers read "Vision" and "Roadmap"
    - id="navHardware" still present exactly once
    - hw-waitlist form untouched
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — full site reframe</name>
  <action>Human verifies the completed site reframe in a browser. All automated grep checks must pass before this checkpoint is reached.</action>
  <what-built>
    Full site reframe: hero is plugin-first, hardware section has no pricing, shop has one card, nav labels updated across all three nav locations (desktop, mobile, footer).
  </what-built>
  <how-to-verify>
    Open the site in a browser (local file or Vercel preview).

    1. Hero: plugin "Get Plugin — CHF 49" button is the filled/magenta primary. No hardware preorder button present. "Explore Free →" is still the ghost text CTA.
    2. Scroll to #pageHardware: status bar reads "IN DEVELOPMENT". No price visible anywhere in the section. Spec table has no "Target price" row. Roadmap timeline "Beta Units" step has no CHF 499. Waitlist form (email input + Notify Me button) is still present.
    3. Scroll to #pageShop: one card visible (Arcade Chord Control). Hardware and bundle cards are gone. Small monospaced "MORE PRODUCTS IN DEVELOPMENT" note visible below the plugin card. Shop description copy no longer mentions hardware pre-orders.
    4. Desktop nav: links read "Plugin · About · Vision · Impressions · Shop · Roadmap" (no "Hardware", no "App").
    5. Click [ MENU ] on mobile/narrow viewport: overlay shows "Vision" and "Roadmap" labels.
    6. Footer: navigate section shows "Vision" and "Roadmap" labels.
    7. Scroll through the full page — hardware section scroll-spy should still highlight the Vision nav link correctly when in view.
    8. Run final grep: `grep -n "499\|[Pp]reorder\|hardware pre-order" index.html` — must return 0 matches.
  </how-to-verify>
  <verify>
    <automated>grep -n "499\|[Pp]reorder\|hardware pre-order" index.html</automated>
  </verify>
  <done>All visual checks pass, user types "approved".</done>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All automated verification passes:
- `grep -n "499\|Preorder\|preorder" index.html` → 0 matches
- `grep -n "shop-card--preorder\|shop-card--dim" index.html` → 0 matches
- `grep -c "id=\"navHardware\"" index.html` → 1 (scroll-spy ID intact)

Manual checkpoint approved by user.
</verification>

<success_criteria>
- No CHF 499 or "preorder" language anywhere visible on the page
- Hero: plugin is the primary CTA, hardware button is gone
- Hardware section: vision framing, no pricing, no purchase CTA, waitlist intact
- Shop: single plugin card + "More coming" note, no hardware or bundle cards
- All three nav locations (desktop, mobile overlay, footer) show "Vision" and "Roadmap"
- Scroll-spy continues to work (navHardware ID preserved)
- Site deploys cleanly to Vercel with no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-site-reframe-plugin-first/10-01-SUMMARY.md` using the summary template.
</output>
