---
phase: 02-about-story-vision
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - assets/hw-origin-performance.jpg
  - assets/hw-origin-schematic.jpg
autonomous: true
requirements:
  - "Hardware origin story founder-voiced intro paragraph"
  - "Fix broken hardware section image paths"

must_haves:
  truths:
    - "Hardware section opens with founder-voiced paragraph starting 'This didn't start as a product. It started as a problem.'"
    - "The existing 3-step origin strip (Modular → Plugin → Hardware) is unchanged"
    - "Performance photo is visible in the hardware origin strip (no broken image)"
    - "Schematic photo is visible in the hardware origin strip (no broken image)"
  artifacts:
    - path: "assets/hw-origin-performance.jpg"
      provides: "Hardware origin performance photo"
    - path: "assets/hw-origin-schematic.jpg"
      provides: "DIMEOLA schematic photo"
    - path: "index.html"
      provides: "Hardware founder intro paragraph and fixed image src attributes"
      contains: "hw-founder-intro"
  key_links:
    - from: ".hw-founder-intro"
      to: "index.html hardware section"
      via: "DOM insertion between .hw-header and .hw-origin"
      pattern: "hw-founder-intro"
    - from: "hw-origin img"
      to: "assets/hw-origin-performance.jpg"
      via: "img src attribute"
      pattern: "src=\"assets/hw-origin-performance.jpg\""
    - from: "hw-origin img"
      to: "assets/hw-origin-schematic.jpg"
      via: "img src attribute"
      pattern: "src=\"assets/hw-origin-schematic.jpg\""
---

<objective>
Enrich the hardware page with a founder-voiced intro paragraph above the existing origin strip, and fix two broken image paths in that same strip. Makes the hardware section feel personal and grounded before the 3-step story begins.

Purpose: Adds emotional context ("this started as a problem") before the product explanation. Fixes broken images that are currently placeholder boxes.
Output: assets/hw-origin-performance.jpg + assets/hw-origin-schematic.jpg copied; .hw-founder-intro paragraph inserted above .hw-origin in index.html; both broken src attributes corrected.
</objective>

<execution_context>
@C:/Users/Milosh Xhavid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Milosh Xhavid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.0-ROADMAP.md
@.planning/milestones/v1.0-phases/02-about-story-vision/02-CONTEXT.md
@.planning/milestones/v1.0-phases/02-about-story-vision/02-RESEARCH.md

<interfaces>
<!-- Key patterns from index.html for the executor. Use these directly. -->

Existing working image path (confirms assets/ is correct relative base):
```html
<img src="assets/arcade-chord-control.png" ...>   <!-- line ~739 -->
```

Hardware section structure (approximate, read file to confirm exact lines):
```html
<div class="hw-header">          <!-- ends ~line 772 -->
  ...
</div>
<!-- ORIGIN STRIP -->            <!-- ~line 773 -->
<div class="hw-origin">          <!-- ~line 774 -->
  ...
  <img src="/mnt/user-data/uploads/WhatsApp_Image_2026-01-17_at_00_04_34.jpeg" ...>  <!-- broken -->
  ...
  <img src="/mnt/user-data/uploads/Dimeola_Schalltplan.jpg" ...>  <!-- broken -->
  ...
</div>
```

CSS variables available: --navy-deep, --navy-mid, --teal, --text, --text-dim, --text-faint, --border

Existing section pattern for reference (hw-section body text):
```css
/* Body copy in hw sections uses font-size:15px, line-height:1.8, color:var(--text-dim) */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy hardware origin images to assets</name>
  <files>assets/hw-origin-performance.jpg, assets/hw-origin-schematic.jpg</files>
  <action>
Copy both hardware origin images to the assets directory using fully quoted paths to handle spaces in filenames and directory names:

```bash
cp "C:/Users/Milosh Xhavid/Desktop/DIMEA Webseite/WhatsApp Image 2026-01-17 at 00.04.34.jpeg" \
   "C:/Users/Milosh Xhavid/dimea-website/assets/hw-origin-performance.jpg"

cp "C:/Users/Milosh Xhavid/Desktop/DIMEA Webseite/Dimeola Schalltplan.jpg" \
   "C:/Users/Milosh Xhavid/dimea-website/assets/hw-origin-schematic.jpg"
```

After each copy, verify non-zero file size:
```bash
ls -lh "C:/Users/Milosh Xhavid/dimea-website/assets/hw-origin-performance.jpg"
ls -lh "C:/Users/Milosh Xhavid/dimea-website/assets/hw-origin-schematic.jpg"
```

If either copy fails or produces a 0-byte file, confirm the exact filename with:
```bash
ls "C:/Users/Milosh Xhavid/Desktop/DIMEA Webseite/"
```
  </action>
  <verify>
    <automated>ls -lh "C:/Users/Milosh Xhavid/dimea-website/assets/hw-origin-performance.jpg" "C:/Users/Milosh Xhavid/dimea-website/assets/hw-origin-schematic.jpg"</automated>
  </verify>
  <done>Both assets/hw-origin-performance.jpg and assets/hw-origin-schematic.jpg exist with non-zero file sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Insert founder intro paragraph and fix broken image src attributes in index.html</name>
  <files>index.html</files>
  <action>
Make three targeted edits to index.html. Use the Read tool to confirm exact line numbers before editing — do NOT rewrite the entire file.

**Edit 1 — Insert .hw-founder-intro div:**
Find the closing `</div>` of `.hw-header` and the start of `.hw-origin` (approximately lines 772-774). Insert the following block between them, after the `.hw-header` closing tag and before the `.hw-origin` div (or its comment):

```html
<!-- ── FOUNDER INTRO ─────────────────────────────────────────── -->
<div class="hw-founder-intro">
  <p class="hw-founder-voice">This didn't start as a product. It started as a problem.</p>
  <p class="hw-founder-expand">A table full of Eurorack modules, an XY joystick, and a trumpet. It worked — beautifully — but it was never something anyone else could just pick up and play.</p>
</div>
```

**Edit 2 — Fix broken performance image src:**
Find the img tag with `src` containing `WhatsApp_Image_2026-01-17_at_00_04_34.jpeg` (broken `/mnt/user-data/uploads/` path). Replace its entire `src` attribute value with `assets/hw-origin-performance.jpg`. Keep all other attributes (alt, class, etc.) unchanged.

**Edit 3 — Fix broken schematic image src:**
Find the img tag with `src` containing `Dimeola_Schalltplan.jpg` (broken `/mnt/user-data/uploads/` path). Replace its entire `src` attribute value with `assets/hw-origin-schematic.jpg`. Keep all other attributes unchanged.

**Edit 4 — Add CSS for .hw-founder-intro (append inside `<style>` block before `</style>`):**
```css
/* ── HW FOUNDER INTRO ──────────────────────────────────────────── */
.hw-founder-intro {
  max-width: 700px;
  margin-bottom: 56px;
}
.hw-founder-voice {
  font-family: 'Syne', sans-serif;
  font-size: clamp(18px, 2vw, 24px);
  font-weight: 600;
  color: var(--text);
  line-height: 1.4;
  margin-bottom: 16px;
}
.hw-founder-expand {
  font-size: 15px;
  line-height: 1.8;
  color: var(--text-dim);
}
```

Anti-patterns to avoid:
- Do NOT touch or modify the `.hw-origin` strip content, captions, or layout — only insert above it
- Do NOT use absolute paths (`/mnt/...` or `C:/...`) in src attributes — use relative `assets/filename.jpg`
- Do NOT add JavaScript
- Do NOT use fixed px for hw-founder-voice font-size — use clamp()
  </action>
  <verify>
    <automated>grep -c "hw-founder-intro\|hw-origin-performance\|hw-origin-schematic" "C:/Users/Milosh Xhavid/dimea-website/index.html"</automated>
  </verify>
  <done>
- index.html contains .hw-founder-intro div with both founder paragraphs
- Opening line "This didn't start as a product. It started as a problem." present verbatim
- Both broken /mnt/user-data/uploads/ src paths replaced with assets/hw-origin-performance.jpg and assets/hw-origin-schematic.jpg
- .hw-founder-voice and .hw-founder-expand CSS present in style block
- .hw-origin strip content is unchanged (3-step layout intact)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Founder-voiced intro paragraph above the hardware origin strip: "This didn't start as a product. It started as a problem." followed by the Eurorack expansion sentence. Both previously broken images (performance photo and DIMEOLA schematic) are now loading from assets/.
  </what-built>
  <how-to-verify>
1. Open index.html in a browser (or live server)
2. Navigate to the Hardware section (#pageHardware)
3. Confirm the founder-voiced paragraph appears ABOVE the 3-step origin strip, not inside it
4. Verify "This didn't start as a product. It started as a problem." renders in larger Syne weight
5. Verify the expansion sentence appears below it in smaller body text
6. Scroll to the origin strip — both images (performance photo and schematic) should load correctly, not show broken image icons
7. Confirm the 3-step origin strip (Modular → Plugin → Hardware) is visually unchanged
8. Resize to mobile (< 900px) — confirm the intro paragraph and images still display correctly
9. Check browser console for any 404 errors on image paths (F12 → Console → Network)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- grep confirms hw-founder-intro present in index.html
- grep confirms assets/hw-origin-performance.jpg and assets/hw-origin-schematic.jpg in index.html (no /mnt/ paths remaining)
- Both image files exist in assets/ with non-zero sizes
- .hw-origin strip content grep shows 3-step layout still intact
- Browser shows no 404 errors for these images
</verification>

<success_criteria>
- Hardware section displays founder-voiced intro with locked opening line verbatim
- Both origin strip images load correctly from assets/
- The existing 3-step origin strip is visually identical to before this plan
- No /mnt/user-data/uploads/ paths remain in index.html
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/02-about-story-vision/02-02-SUMMARY.md`
</output>
